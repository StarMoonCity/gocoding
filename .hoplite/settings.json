{
  "version": 1,
  "ports": {"preview": 3000, "additional": {}},
  "scripts": {
    "setup": {
      "enabled": true,
      "command": "export PATH=\"$HOME/.local/share/mise/shims:$PATH\" && (mise install go@1.24 >/dev/null 2>&1 || true) && (mise use -g go@1.24 >/dev/null 2>&1 || true) && (apt-get update -qq >/dev/null 2>&1 || true) && (apt-get install -y git-lfs >/dev/null 2>&1 || true)"
    },
    "run": {"enabled": true, "command": null},
    "archive": {"enabled": true, "command": null},
    "check": {"enabled": true, "command": null}
  },
  "mcpServers": []
}
